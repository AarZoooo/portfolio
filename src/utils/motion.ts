/* Shared animation timing for JS-driven motion. Mirrors the CSS
   duration tokens in src/design/tokens/tokens.css so scripted
   animation stays in sync with the transitions it accompanies. */

/** Matches --duration-medium. */
export const DURATION_MEDIUM_MS = 280
